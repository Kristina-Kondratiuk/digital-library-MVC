<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Error</title>
    <link rel="stylesheet" href="/css/main.css" />
  </head>
  <body>
    <div class="container" style="text-align:center; margin-top:64px;">
      <h1 style="font-size:54px; color:#a02a2a;">Error</h1>
      <h2 style="font-weight:600;">Something went wrong</h2>
      <% if (typeof message !== "undefined" && message) { %>
        <p><%= message %></p>
      <% } else { %>
        <p>Please try again later.</p>
      <% } %>
      <a href="/" class="btn secondary" style="margin-top:20px;">Go Home</a>
    </div>
  </body>
</html>
